---
# tasks file for docker-setup

- name: Get uptime information
  shell: /usr/bin/uptime
  register: result
  changed_when: false

- name: Display debug message
  debug:
    msg:
      - "Uptime for machine is {{ result.stdout }}."
  changed_when: false
